spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/shoestore
spring.datasource.username=root
spring.datasource.password=leminhduc472005
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.show-sql: true

#config spring security
# spring.security.user.name=minhduc
# spring.security.user.password=123456

#default = 1MB
spring.servlet.multipart.max-file-size=50MB
#default = 10 MB (form data)
spring.servlet.multipart.max-request-size=50MB
logging.level.org.springframework.security=DEBUG

spring.session.store-type=jdbc
spring.session.timeout=30m
spring.session.jdbc.initialize-schema=always
#server.servlet.session.timeout
#spring.session.jdbc.table-name=SPRING_SESSION

# VNPAY
hoidanit.vnpay.tmn-code=${VNPAY_TMN_CODE}
hoidanit.vnpay.hash-secret=${VNPAY_HASH_SECRET}
hoidanit.vnpay.vnp-url=${VNPAY_URL}
hoidanit.vnpay.vnp-return-url=${VNPAY_RETURN_URL}

# OAuth2 Google
spring.security.oauth2.client.registration.google.client-id=${OAUTH2_GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${OAUTH2_GOOGLE_CLIENT_SECRET}

# OAuth2 GitHub
spring.security.oauth2.client.registration.github.client-id=${OAUTH2_GITHUB_CLIENT_ID}
spring.security.oauth2.client.registration.github.client-secret=${OAUTH2_GITHUB_CLIENT_SECRET}

# Elasticsearch
spring.elasticsearch.uris=${ELASTIC_URL}
spring.elasticsearch.index.auto-create=false

# Giữ lại nếu cần
spring.main.allow-bean-definition-overriding=true
